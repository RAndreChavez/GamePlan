PORT=5173
SESSION_SECRET=change-this-before-deployment
NODE_ENV=development
# Optional: enables real route geometry/ETA through OpenRouteService.
# Without this key, GamePlan falls back to a straight-line route preview.
OPENROUTE_SERVICE_API_KEY=
